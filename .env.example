# 复制此文件并重命名为.env以使用

# Redis 配置
REDIS_HOST=redis  # Docker环境使用redis服务名，本地开发使用127.0.0.1
REDIS_PORT=6379
REDIS_DB=0
REDIS_DECODE_RESPONSES=True

# 数据库配置选项

# 本地开发 - 使用SQLite（无需额外配置）
# DATABASE_URL=sqlite://./db.sqlite3

# Docker环境 - 使用内置MySQL（与docker-compose.yml配置一致）
# DATABASE_URL=mysql://appuser:secret@db:3306/messageboard
# GENERATE_SCHEMAS=True  # 首次启动时生成表结构

# 生产环境 - 使用远程MySQL（当前硬编码值）
DATABASE_URL=mysql://Wang:A19356756837@52.196.78.16:3306/messageboard
GENERATE_SCHEMAS=false  # 表已存在时设为false

# 安全配置
SECRET_KEY=your-secret-key-for-production-change-this
ACCESS_TOKEN_EXPIRE_MINUTES=30

# 使用说明：
# 1. 复制此文件为.env并根据需要取消注释相应配置
# 2. 本地开发推荐使用SQLite或本地MySQL
# 3. Docker部署请使用docker-compose.yml中定义的数据库配置
# 4. 生产环境务必更换SECRET_KEY为安全随机值
# 5. 所有依赖包已包含在requirements.txt中，可通过pip install -r requirements.txt安装
